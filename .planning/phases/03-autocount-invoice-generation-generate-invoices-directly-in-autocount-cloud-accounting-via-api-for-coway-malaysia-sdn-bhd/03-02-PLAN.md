---
phase: 03-autocount-invoice-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - billing-app/src/app/billing/generate-invoice/page.tsx
autonomous: true
requirements:
  - INV-08

must_haves:
  truths:
    - "User can navigate to /billing/generate-invoice"
    - "User can select customer and billing month"
    - "User can click generate and see invoice result"
    - "Error messages shown for failures"
  artifacts:
    - path: "billing-app/src/app/billing/generate-invoice/page.tsx"
      provides: "Generate Invoice UI page"
      exports: ["default page component"]
  key_links:
    - from: "billing-app/src/app/billing/generate-invoice/page.tsx"
      to: "billing-app/src/app/api/invoices/generate-auto/route.ts"
      via: "fetch POST /api/invoices/generate-auto"
---

<objective>
Create the Generate Invoice UI page at /billing/generate-invoice

Purpose: Allow users to generate invoices via a simple UI that calls the API
Output: Working UI page for invoice generation
</objective>

<execution_context>
@/Users/thyeonyam/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
# UI patterns from existing billing page:
# - Use Shadcn UI components (Card, Button, Select, Input, Label, Alert)
# - Use lucide-react icons (FileText, Loader2, CheckCircle2, XCircle)
# - State management with useState
# - Fetch data from API and display results

# Key components used:
- Card, CardContent, CardHeader, CardTitle from @/components/ui/card
- Button from @/components/ui/button
- Select, SelectContent, SelectItem, SelectTrigger, SelectValue from @/components/ui/select
- Input from @/components/ui/input
- Label from @/components/ui/label
- Alert, AlertDescription, AlertTitle, Alert variant from @/components/ui/alert

# API contract from Plan 1:
POST /api/invoices/generate-auto
Request: { customerId: string, billingMonth: string }
Response: { success: boolean; invoice?: { id, customerId, billingMonth, totalAmount, status, autocountRefId?, createdAt }; error?: string }
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Generate Invoice UI page</name>
  <files>billing-app/src/app/billing/generate-invoice/page.tsx</files>
  <action>
    Create a new page at /billing/generate-invoice for simplified invoice generation.

    Implementation:
    1. Create directory: billing-app/src/app/billing/generate-invoice/
    2. Create page.tsx with:

    UI Components:
    - Header: "Generate Invoice" title
    - Customer Select dropdown (hardcode "Coway (Malaysia) Sdn Bhd" for v1, or fetch from API)
    - Billing Month input (type="month")
    - "Generate Invoice" button
    - Loading state with spinner
    - Success/Error alert display

    State:
    - customerId: string (default: "coway-malaysia" or MongoDB customer id)
    - billingMonth: string (default: current month in YYYY-MM format)
    - isLoading: boolean
    - result: { success: boolean; message: string; docNo?: string } | null

    Handler:
    - On submit: POST /api/invoices/generate-auto with { customerId, billingMonth }
    - Show loading while waiting
    - Display success with AutoCount docNo or error message
    - Format: "Invoice generated successfully. AutoCount Doc No: {docNo}"

    Style:
    - Use Card component for container
    - Use same styling patterns as /billing page
    - Center the form with max-width

    Notes:
    - This is a simplified flow compared to /billing which allows editing line items
    - For v1 (Coway only), customer selection can be hardcoded or dropdown with single option
  </action>
  <verify>
    <automated>TypeScript compilation check: npx tsc --noEmit --project billing-app/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>UI page exists at /billing/generate-invoice, allows customer + month selection, generates invoice via API, displays result</done>
</task>

</tasks>

<verification>
- Page compiles without errors
- Form inputs work correctly
- API call made on submit
- Result displayed (success/error)
</verification>

<success_criteria>
- [x] Page created at /billing/generate-invoice
- [x] Customer selection (hardcoded or dropdown)
- [x] Billing month selection
- [x] Generate button calls API
- [x] Success shows AutoCount doc number
- [x] Error shows failure reason
</success_criteria>

<output>
After completion, create `.planning/phases/03-autocount-invoice-generation/03-02-SUMMARY.md`
</output>

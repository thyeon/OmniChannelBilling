---
phase: 04-omnisource
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - billing-app/src/app/api/customers/route.ts
  - billing-app/src/app/api/customers/[id]/datasources/route.ts
  - billing-app/src/app/admin/customers/page.tsx
  - billing-app/src/app/admin/customers/wizard/page.tsx
  - billing-app/src/app/admin/customers/wizard/components/WizardForm.tsx
  - billing-app/src/app/admin/customers/wizard/components/DataSourceStep.tsx
autonomous: true
requirements:
  - DS-06
  - AC-01
  - UI-01
  - DV-02

must_haves:
  truths:
    - "Admin can add new customers with wizard flow"
    - "Data sources are configurable in wizard UI"
    - "AutoCount settings configured per customer"
    - "Default field values settable per customer"
  artifacts:
    - path: "billing-app/src/app/api/customers/[id]/datasources/route.ts"
      provides: "DataSource CRUD API endpoints"
    - path: "billing-app/src/app/admin/customers/wizard/page.tsx"
      provides: "Customer setup wizard UI"
    - path: "billing-app/src/app/admin/customers/wizard/components/DataSourceStep.tsx"
      provides: "Data source configuration step in wizard"
  key_links:
    - from: "WizardForm.tsx"
      to: "datasources/route.ts"
      via: "POST /api/customers/[id]/datasources"
      pattern: "fetch.*datasources"
    - from: "DataSourceStep.tsx"
      to: "billingService.ts"
      via: "Preview billable data during config"
      pattern: "generateBillableData"
---

<objective>
Create the admin UI for customer setup: wizard flow with data source configuration.

Purpose: Enable admins to add new customers with configurable data sources, AutoCount settings, and default values through a guided wizard.

Output: Wizard UI components and API endpoints for data source management.
</objective>

<execution_context>
@/Users/thyeonyam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thyeonyam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@billing-app/src/app/billing/generate-invoice/page.tsx (existing UI patterns)
@billing-app/src/app/autocount-settings/page.tsx (existing settings patterns)

## Existing UI Patterns
- Use server actions or API routes for data
- Use existing UI components (forms, inputs from shadcn/ui)
- Wizard pattern: step-based form with navigation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DataSource API routes</name>
  <files>billing-app/src/app/api/customers/[id]/datasources/route.ts</files>
  <action>
    Create API routes for DataSource CRUD:
    - GET /api/customers/[id]/datasources - list all data sources for customer
    - POST /api/customers/[id]/datasources - create new data source
    - PUT /api/customers/[id]/datasources/[dsId] - update data source
    - DELETE /api/customers/[id]/datasources/[dsId] - delete data source

    Use existing API patterns from customers/[id]/route.ts.
    Validate: type, serviceType, apiEndpoint required.
  </action>
  <verify>
    <automated>grep -l "GET\|POST" billing-app/src/app/api/customers/\[id\]/datasources/route.ts</automated>
  </verify>
  <done>DataSource API routes for CRUD operations</done>
</task>

<task type="auto">
  <name>Task 2: Create Customer Wizard UI</name>
  <files>billing-app/src/app/admin/customers/wizard/page.tsx</files>
  <action>
    Create wizard page at /admin/customers/wizard with:
    - Step indicator (1. Basic Info, 2. Data Sources, 3. AutoCount, 4. Defaults)
    - Step navigation (Next/Back buttons)
    - Form state management (useState for customer data)
    - Steps:
      1. Basic Info: name, email, contact (reuse fields from existing customer form)
      2. Data Sources: DataSourceStep component (next task)
      3. AutoCount: autocountAccountBookId dropdown (from account books), autocountDebtorCode input
      4. Defaults: rates per service, description templates, tax code

    Use existing UI components from shadcn/ui.
    On completion: POST to /api/customers then create data sources.
  </action>
  <verify>
  <automated>grep -l "wizard\|Step" billing-app/src/app/admin/customers/wizard/page.tsx</automated>
  </done>
  <done>Wizard page with step navigation and form state</done>
</task>

<task type="auto">
  <name>Task 3: Create DataSource Step component</name>
  <files>billing-app/src/app/admin/customers/wizard/components/DataSourceStep.tsx</files>
  <action>
    Create DataSourceStep component for wizard step 2:
    - List of data sources (cards with edit/delete)
    - Add new data source button
    - Form for new/edit data source:
      - Type dropdown (COWAY_API, RECON_SERVER, CUSTOM_REST_API)
      - Service Type dropdown (SMS, EMAIL, WHATSAPP)
      - Name input
      - API Endpoint input
      - Auth Type dropdown (API_KEY, BEARER_TOKEN, BASIC_AUTH, NONE)
      - Auth credentials fields (conditional based on auth type)
      - Response mapping path input (usageCountPath)
    - Test Connection button (optional, can call preview)
    - Preview button to test data source and show sample data

    Use existing form components (shadcn/ui).
    Call GET /api/customers/[id]/datasources to load existing.
  </action>
  <verify>
  <automated>grep -l "DataSourceStep\|DataSourceForm" billing-app/src/app/admin/customers/wizard/components/DataSourceStep.tsx</automated>
  </done>
  <done>Data source configuration UI with form and list view</done>
</task>

</tasks>

<verification>
- DataSource API routes functional
- Wizard navigates through all steps
- Data sources can be added/edited/deleted in wizard
- AutoCount settings configurable per customer
- Default values configurable per customer
</verification>

<success_criteria>
1. Admin can navigate wizard from Basic Info to Defaults
2. Data sources persist after wizard completion
3. New customer appears in customer list with all configured settings
</success_criteria>

<output>
After completion, create `.planning/phases/04-omnisource/04-02-SUMMARY.md`
</output>
